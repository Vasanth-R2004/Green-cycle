/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com', 'images.unsplash.com', 'unsplash.com','media.istockphoto.com','plus.unsplash.com', 'media.wired.com'], 
      },
}

module.exports = nextConfig